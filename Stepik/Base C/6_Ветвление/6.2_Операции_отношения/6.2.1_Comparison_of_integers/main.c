#include <stdio.h>

int   main(void)
{
  int nbr_one = 0, nbr_two = 0, res = 0;

  scanf("%d %d", &nbr_one, &nbr_two);
  nbr_one == nbr_two ? (res = 1) : (res = 0);
  printf("%d\n", res = 1);
  
  return (0);
}