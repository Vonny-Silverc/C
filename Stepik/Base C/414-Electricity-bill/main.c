#include <stdio.h>

int       main(void)
{
  int     mtr_inp = 0, mtr_out = 0;
  double  math = 0, result = 0;
    
  scanf("%d %d %lf", &mtr_inp, &mtr_out, &math);
  printf("%.2lf", result = (mtr_out - mtr_inp) * math);
    
  return (0);
}